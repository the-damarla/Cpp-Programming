#include <iostream>

using namespace std;

int main()
{
    char x;
    cout << "Enter a character to find its ASCII Value : ";
    cin >> x;
    cout << "ASCII Value of " << x << " is " << int(x);
    return 0;
}
